import { TablePayment } from './Table';

import type { Meta } from '@storybook/react';

const meta: Meta<typeof TablePayment> = { component: TablePayment, title: 'Features/Table Payment' };

export default meta;
export const Overview = { args: {} };