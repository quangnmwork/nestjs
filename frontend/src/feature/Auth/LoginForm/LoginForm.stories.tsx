import { LoginForm } from './LoginForm';
import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof LoginForm> = { component: LoginForm, title: "Features/LoginForm" };

export default meta;
export const Overview = { args: {} };